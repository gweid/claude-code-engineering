# Orders API

> Auto-generated by api-generating Skill. Do not edit manually.

## Base Path

`/api/orders`

## Route Source

- `/Users/gweid/Desktop/claude-code-engineering/03-Skills/06-agent-skill-combo/src/routes/order.js`

## Notes

- 当前仓库的 `src/` 目录中未包含 `../schemas/order` 与相关中间件实现，因此 `validate(orderSchema)` 的精确字段规则无法从本次扫描中完全确定。
- `GET /api/orders/:id/status` 只检查订单是否存在，没有检查该订单是否属于当前用户；这意味着状态读取接口的授权粒度与订单详情接口不同。
- `PUT /api/orders/:id/status` 仅要求已登录，代码中未包含所有权检查或状态值校验。

---

## Endpoints

### GET `/api/orders`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`

**Description**: 获取当前登录用户的订单列表。

**Parameters**:

无路径参数、查询参数或请求体。

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 返回当前用户的订单列表 |
| 401 | 未认证，来自 `requireAuth` |

**Response Example**:

```json
{
  "data": [
    {
      "id": "order_1",
      "userId": "user_123",
      "status": "pending"
    }
  ]
}
```

### GET `/api/orders/:id`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`

**Description**: 获取指定订单详情，仅允许访问自己的订单。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | 是 | 订单 ID |

**Request Body**: 无

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 返回订单详情 |
| 401 | 未认证，来自 `requireAuth` |
| 403 | 订单不属于当前用户，返回 `{"error":"Forbidden"}` |
| 404 | 订单不存在，返回 `{"error":"Order not found"}` |

**Response Example**:

```json
{
  "data": {
    "id": "order_123",
    "userId": "user_123",
    "status": "pending"
  }
}
```

### POST `/api/orders`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`
- `validate(orderSchema)`

**Description**: 创建新订单，服务端会强制写入当前登录用户的 `userId`，并将状态设置为 `pending`。

**Parameters**:

无路径参数或查询参数。

**Request Body**:

- 请求体会被 `orderSchema` 校验。
- 代码会将 `req.body` 展开后覆盖追加：`userId = req.user.id`、`status = "pending"`。
- 由于 `src/` 中缺少 schema 定义文件，以下请求体示例仅表示订单业务字段的占位形式。

```json
{
  "items": [
    {
      "productId": "prod_1",
      "quantity": 2
    }
  ],
  "shippingAddress": "Some Address"
}
```

**Response**:

| Status | Description |
|--------|-------------|
| 201 | 创建成功，返回新订单 |
| 400 | 请求体未通过 `orderSchema` 校验 |
| 401 | 未认证，来自 `requireAuth` |

**Response Example**:

```json
{
  "data": {
    "id": "order_123",
    "userId": "user_123",
    "status": "pending",
    "items": [
      {
        "productId": "prod_1",
        "quantity": 2
      }
    ],
    "shippingAddress": "Some Address"
  }
}
```

### GET `/api/orders/:id/status`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`

**Description**: 获取指定订单状态。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | 是 | 订单 ID |

**Request Body**: 无

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 返回订单状态 |
| 401 | 未认证，来自 `requireAuth` |
| 404 | 订单不存在，返回 `{"error":"Order not found"}` |

**Response Example**:

```json
{
  "status": "pending"
}
```

### PUT `/api/orders/:id/status`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`

**Description**: 更新指定订单状态。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | 是 | 订单 ID |
| status | body | string | 是 | 目标订单状态，直接取自 `req.body.status` |

**Request Body**:

```json
{
  "status": "shipped"
}
```

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 更新成功，返回更新后的订单 |
| 401 | 未认证，来自 `requireAuth` |

**Response Example**:

```json
{
  "data": {
    "id": "order_123",
    "userId": "user_123",
    "status": "shipped"
  }
}
```

---

## Error Codes

| Code | Message | Description |
|------|---------|-------------|
| 400 | Bad Request | 创建订单校验失败，或状态值不合法时可能由下层逻辑返回 |
| 401 | Unauthorized | 缺少认证或认证无效 |
| 403 | Forbidden | 访问不属于当前用户的订单详情 |
| 404 | Order not found | 指定订单不存在 |
| 500 | Internal Server Error | 代码中未显式处理的异常 |
