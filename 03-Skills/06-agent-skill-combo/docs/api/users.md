# Users API

> Auto-generated by api-generating Skill. Do not edit manually.

## Base Path

`/api/users`

## Route Source

- `/Users/gweid/Desktop/claude-code-engineering/03-Skills/06-agent-skill-combo/src/routes/user.js`

## Notes

- `POST /api/users` 和 `PUT /api/users/:id` 使用 `validate(...)` 中间件，但当前仓库的 `src/` 目录中未包含对应 schema 文件，因此无法从本次扫描中精确还原完整字段约束。
- `GET /api/users` 中的 `page`、`limit` 来自 `req.query`，代码未做数值转换；响应中的 `page`、`limit` 可能保持为字符串。

---

## Endpoints

### GET `/api/users`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`
- `isAdmin`

**Description**: 获取用户列表，仅管理员可访问。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| page | query | string \| number | 否 | 页码，默认值为 `1` |
| limit | query | string \| number | 否 | 每页数量，默认值为 `20` |

**Request Body**: 无

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 返回用户列表以及分页参数 |
| 401 | 未认证，来自 `requireAuth` |
| 403 | 非管理员，来自 `isAdmin` |

**Response Example**:

```json
{
  "data": [
    {
      "id": "user_1"
    },
    {
      "id": "user_2"
    }
  ],
  "page": "1",
  "limit": "20"
}
```

### GET `/api/users/:id`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`

**Description**: 根据用户 ID 获取单个用户。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | 是 | 用户 ID |

**Request Body**: 无

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 返回用户详情 |
| 401 | 未认证，来自 `requireAuth` |
| 404 | 用户不存在，返回 `{"error":"User not found"}` |

**Response Example**:

```json
{
  "data": {
    "id": "user_123"
  }
}
```

### POST `/api/users`

**Authorization / Middleware**:
- `validate(userSchema)`

**Description**: 创建新用户。

**Parameters**:

无路径参数或查询参数。

**Request Body**:

- 请求体会被 `userSchema` 校验。
- 由于 `src/` 中缺少 schema 定义文件，以下示例仅表示“提交一个用户对象”，不代表完整字段约束。

```json
{
  "name": "Alice",
  "email": "alice@example.com"
}
```

**Response**:

| Status | Description |
|--------|-------------|
| 201 | 创建成功，返回新用户 |
| 400 | 请求体未通过 `userSchema` 校验 |

**Response Example**:

```json
{
  "data": {
    "id": "user_123",
    "name": "Alice",
    "email": "alice@example.com"
  }
}
```

### PUT `/api/users/:id`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`
- `validate(updateUserSchema)`

**Description**: 更新指定用户。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | 是 | 用户 ID |

**Request Body**:

- 请求体会被 `updateUserSchema` 校验。
- 由于 `src/` 中缺少 schema 定义文件，以下示例仅为可更新字段的示意。

```json
{
  "name": "Alice Updated"
}
```

**Response**:

| Status | Description |
|--------|-------------|
| 200 | 更新成功，返回更新后的用户 |
| 400 | 请求体未通过 `updateUserSchema` 校验 |
| 401 | 未认证，来自 `requireAuth` |
| 404 | 用户不存在，返回 `{"error":"User not found"}` |

**Response Example**:

```json
{
  "data": {
    "id": "user_123",
    "name": "Alice Updated"
  }
}
```

### DELETE `/api/users/:id`

🔒 Requires authentication

**Authorization / Middleware**:
- `requireAuth`
- `isAdmin`

**Description**: 删除指定用户，仅管理员可访问。

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | 是 | 用户 ID |

**Request Body**: 无

**Response**:

| Status | Description |
|--------|-------------|
| 204 | 删除成功，无响应体 |
| 401 | 未认证，来自 `requireAuth` |
| 403 | 非管理员，来自 `isAdmin` |

**Response Example**:

```json
null
```

---

## Error Codes

| Code | Message | Description |
|------|---------|-------------|
| 400 | Bad Request | 校验失败时由 `validate(...)` 返回 |
| 401 | Unauthorized | 缺少认证或认证无效 |
| 403 | Forbidden | 非管理员访问管理员接口 |
| 404 | User not found | 指定用户不存在 |
| 500 | Internal Server Error | 代码中未显式处理的异常 |
